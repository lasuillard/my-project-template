#!/usr/bin/env bash

# This script is run after the `onCreateCommand` if new content is available, such as code changes.
# You could use it to update dependencies, run migrations, or perform other tasks that need to be done when the content changes.

make install
