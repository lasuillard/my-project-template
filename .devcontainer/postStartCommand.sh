#!/usr/bin/env bash

# This script is run each time the dev container is started.
# You could start some services here, or run some commands that need to be executed every time the container starts.

make init
